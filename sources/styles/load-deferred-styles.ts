/**
 * Deferred chunk: Bulma remainder + main + recolors (+ bulma-overrides-rest). Critical entry
 * already includes bulma-critical, shell, and bulma-overrides-paint for first paint.
 */
import "./deferred-entry.scss";
