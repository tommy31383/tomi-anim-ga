/**
 * Default URL hash (without leading `#`) for ZIP export profiling so the
 * character includes layered gear (shoulders, clothes, weapon, etc.) when the
 * fragment would otherwise be empty.
 *
 * Used by `zip-export-profile.js` (Playwright) and `zip-export-profile-runner.js`.
 */
export const ZIP_PROFILE_DEFAULT_HASH =
  "sex=male&body=Body_Color_light&head=Human_Male_light&expression=Neutral_light&shoulders=Legion_gold&legs=Pantaloons_purple&clothes=Longsleeve_forest&weapon=Longsword_longsword";
